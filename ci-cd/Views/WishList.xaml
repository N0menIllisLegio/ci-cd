<UserControl
    x:Class="ci_cd.Views.WishList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
  <Grid>
    <ListView ItemsSource="{Binding Games}">
      <ListView.View>
        <GridView>
          <GridViewColumn DisplayMemberBinding="{Binding Title}">Game</GridViewColumn>
          <GridViewColumn DisplayMemberBinding="{Binding Price}">Price</GridViewColumn>
        </GridView>
      </ListView.View>
    </ListView>
  </Grid>
</UserControl>
